# @import(./apps/server/, pick=[NODE_ENV, BETTER_AUTH_*, CORS_ORIGIN, SMTP_*, EMAIL_FROM, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, DATABASE_URL, REDIS_URL])
# @generateTsTypes(path=./env.ts, exposeEnv=local)